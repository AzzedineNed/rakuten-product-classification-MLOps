# ---------------------------------------------------------------------------
# Rakuten image-classification MLOps pipeline - Python deps
# Python 3.10/3.11 recommended.
#
# IMPORTANT: install PyTorch separately FIRST, because the right wheel depends
# on your hardware. PyTorch is intentionally NOT pinned below.
#
#   CPU only (recommended for this pipeline, incl. laptops):
#     pip install torch==2.2.2 torchvision==0.17.2 \
#       --index-url https://download.pytorch.org/whl/cpu
#
#   CUDA 12.1 (only if you have a working GPU + drivers):
#     pip install torch==2.2.2 torchvision==0.17.2 \
#       --index-url https://download.pytorch.org/whl/cu121
#
# Then:  pip install -r requirements.txt
# ---------------------------------------------------------------------------

numpy==1.26.4
pandas==2.2.2
pillow==10.3.0
scikit-learn==1.4.2
# scipy arrives as a scikit-learn dependency and was previously left to resolve
# to whatever was current. It is pinned deliberately because it supplies the
# L-BFGS-B implementation that actually fits LogisticRegression: a solver change
# can shift fitted coefficients, and every metric this project reports with them,
# without a single line of our own code changing. 1.17.1 is the version all the
# recorded numbers were produced with.
#
# This is a REPRODUCIBILITY pin, not a breakage guard. scipy deprecates the
# disp/iprint options that scikit-learn 1.4.2 passes to L-BFGS-B, but unknown
# solver options only produce OptimizeWarning and the fit still succeeds
# (measured) - removal will not turn the suite red.
scipy==1.17.1
joblib==1.4.2
tqdm==4.66.4
matplotlib==3.8.4
requests==2.31.0
python-dotenv==1.0.1

# API
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9

# Dev / tests
pytest==8.2.0
# fastapi's TestClient transport, used by tests/test_text_api.py. Test-only,
# the running services never import it. Pinned because requirements-ci.txt
# installs it and check_ci_pins.py requires every CI pin to exist here.
httpx==0.28.1

# Observability (Prometheus exposition for the three FastAPI services).
# ZERO transitive dependencies, which is why it was chosen over
# prometheus-fastapi-instrumentator: that package's current release requires
# starlette>=1.0.0 and silently upgrades starlette 0.37.2 -> 1.6.0, breaking
# fastapi==0.111.0. See src/rakuten_common/observability.py.
prometheus-client==0.26.0

# Experiment tracking & data versioning (DagsHub-hosted)
mlflow==3.14.0
dagshub==0.7.0

# Data & model versioning (DVC over DagsHub HTTP remote)
dvc==3.67.1
dvc-http==2.32.0
